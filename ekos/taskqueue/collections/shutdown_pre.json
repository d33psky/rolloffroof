{
    "name": "Roll-off Roof Shutdown (pre-Ekos-stop)",
    "description": "Calls observatory-close-pre: park mount, start warming, close roof (order is safety-critical; handled inside the script). failure_action 1 so the post queue (cooler-off) always runs even if this aborts.",
    "version": "1.0",
    "tasks": [
        {
            "template_id": "script_execute",
            "device": "",
            "failure_action": 1,
            "parameters": {
                "script_path": "/home/hans/src/rolloffroof/ekos/observatory-close-pre",
                "timeout": 900
            }
        }
    ]
}
