{
    "name": "Roll-off Roof Startup (pre-Ekos)",
    "description": "Runs before the Ekos profile starts (devices are already connected via the standalone indiserver). Begins cooling the main camera as early as possible; the Capture module waits for the setpoint before exposures.",
    "version": "1.0",
    "tasks": [
        {
            "template_id": "script_execute",
            "device": "",
            "failure_action": 1,
            "parameters": {
                "script_path": "/home/hans/src/rolloffroof/bin/obsy-report-startup-pre-start",
                "timeout": 30
            }
        },
        {
            "template_id": "camera_setpoint",
            "device": "ZWO CCD ASI2600MM Pro",
            "failure_action": 1,
            "parameters": {
                "target_temperature": -10,
                "ramp_slope": 3,
                "ramp_threshold": 0.2
            }
        }
    ]
}
